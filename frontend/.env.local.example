# URL the Next.js server uses to proxy /api/* to the FastAPI backend (server-side rewrite).
BACKEND_URL=http://localhost:8000
# Optional: override the client API base (defaults to /api/v1 via the rewrite).
# NEXT_PUBLIC_API_BASE=/api/v1
